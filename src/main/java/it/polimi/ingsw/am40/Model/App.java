package it.polimi.ingsw.am40.Model;
import it.polimi.ingsw.am40.Controller.Controller;

public class App
{
    public static void main( String[] args ) {
/*
        int numPlayers = 3;
        Game game = new Game(numPlayers);
        Player p1 = new Player("io");
        Player p2 = new Player("tu");
        Player p3 = new Player("egli");
        game.addPlayer(p1);
        game.addPlayer(p2);
        game.addPlayer(p3);
        System.out.println(game.getPlayers().get(0).getNickname());
        System.out.println(game.getPlayers().get(1).getNickname());
        System.out.println(game.getPlayers().get(2).getNickname());
        game.configureGame();
        game.startGame();
        Controller controller = new Controller(game);
        // SELECTION, PICKING, ORDERING, INSERT
        Position pos = new Position(0,0);
        System.out.println(game.getBoard().getGrid().containsKey(pos.getKey()));
        controller.pick(pos);
        System.out.println(game.getCurrentPlayer().getTilesPicked().get(0).getColor().toString());
        System.out.println(game.getCurrentPlayer().getNickname());
        controller.changePlayer();
        System.out.println(game.getCurrentPlayer().getNickname());
*/
    }


}